import { PagePlaceholder } from "../components/page-placeholder";

export default function ModulesPage() {
  return (
    <PagePlaceholder
      title="Modules"
      step="Tilted Card grid + module detail pages land in build-order step 5."
    />
  );
}
